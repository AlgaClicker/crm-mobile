import 'package:flutter/material.dart';

class MasterSpecificationsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Спецификации')),
      body: const Center(
        child: Text('Список спецификаций'),
      ),
    );
  }
}
