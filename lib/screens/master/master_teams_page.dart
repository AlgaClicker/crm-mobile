import 'package:flutter/material.dart';

class MasterTeamsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Бригады')),
      body: const Center(
        child: Text('Список бригад'),
      ),
    );
  }
}
